version: "3.9"

services:
  frontend:
    build: .
    container_name: ibr-tetfund-client
    ports:
      - "3000:3000"

    # This enables hot reload
    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - NODE_ENV=development

    command: pnpm dev